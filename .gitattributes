# Auto detect text files and perform LF normalization
* text=auto

# Files excluded from distribution zips built by bin/build-zip.command.
/.gitattributes  export-ignore
/.gitignore      export-ignore
/.nova/          export-ignore
